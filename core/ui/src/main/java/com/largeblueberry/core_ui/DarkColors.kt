package com.largeblueberry.core_ui

import androidx.compose.ui.graphics.Color

object DarkColors {
    val AppBackground = Color(0xFF121212) // 다크 배경
    val AppWhite = Color(0xFF000000) // 다크모드에서는 검정
    val AppBlack = Color(0xFFFFFFFF) // 다크모드에서는 흰색
    val AppRed = Color(0xFFFF6B6B) // 조금 더 밝은 빨강
    val AppPrimaryBlue = Color(0xFF6BA3FF) // 조금 더 밝은 파랑


    // 텍스트 색상
    val AppTextDark = Color(0xFFE0E0E0) // 밝은 회색
    val CardViewMainText = Color(0xFF60A5FA) // 밝은 파랑
    val CardViewSubText = Color(0xFFA1B5D8) // 밝은 회색-파랑


    // 특정 목적의 색상
    val RecordingRed = Color(0xFFFF6B6B)
    val ProgressBarBackgroundTint = Color(0xFF374151) // 어두운 회색
    val CardViewBackGround = Color(0xFF1E40AF) // 어두운 파랑

    // Setting screen colors
    val SettingUtilColor = Color(0xFFB0B0B0) // 밝은 회색
    val SettingItemMainText = Color(0xFFE0E0E0) // 밝은 회색
    val UtilTextColor = Color(0xFFA0A0A0) // 중간 회색
    val SettingBackground = Color(0xFF1F2937) // 어두운 배경
    val SettingBasicUser = Color(0xFF60A5FA) // 밝은 파랑

    // Login screen colors
    val GoogleDisabledContainerColor = Color(0xFF374151) // 어두운 회색
    val GoogleDisabledContentColor = Color(0xFF6B7280) // 중간 회색
    val GoogleButtonBorderColor = Color(0xFF4B5563) // 어두운 테두리
    val UtilHorizontalDivider = Color(0xFF374151) // 어두운 구분선
}